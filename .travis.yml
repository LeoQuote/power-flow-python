language: python
python:
    - "3.2"
    - "3.3"
    - "3.4"
    - "nightly"
# command to install dependencies
install:
    - pip install -r requirements.txt
# command to run tests
script: python generate_node_admittance.py
